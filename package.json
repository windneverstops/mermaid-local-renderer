{
  "scripts": {
    "dev": "cross-env NODE_ENV=development node renderer/server.js",
    "_build": "vite build --config renderer/vite.config.ts && vite build --ssr renderer/src/entry-server.tsx --config renderer/vite.config.ts",
    "_start": "cross-env NODE_ENV=production node renderer/server.js",
    "start": "npm run _build && npm run _start"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}